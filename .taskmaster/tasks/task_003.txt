# Task ID: 3
# Title: Build Core Task Management Operations
# Status: pending
# Dependencies: 2
# Priority: high
# Description: Implement CRUD operations for tasks including create, read, update, delete, and list functionality
# Details:
Create TaskManager class with methods: loadTasks() to read tasks.json, saveTasks() to write tasks.json with atomic writes, getTask(id) to retrieve single task, getAllTasks() with filtering options, createTask(taskData) with ID generation, updateTask(id, updates) with validation, deleteTask(id) with dependency checking. Implement proper error handling for file operations. Add transaction-like behavior to prevent data corruption. Create backup before modifications.

# Test Strategy:
Test all CRUD operations with sample data, verify atomic writes prevent corruption during failures, test filtering by status and priority, ensure dependency constraints are enforced on delete, verify backup creation before modifications, test concurrent access scenarios
